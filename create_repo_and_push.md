# GitHub 저장소 생성 및 푸시 가이드

## 1단계: GitHub에서 저장소 생성

1. 브라우저에서 https://github.com/new 접속
2. Repository name에 `jinasja88-svg` 입력
3. Public 또는 Private 선택
4. **"Initialize this repository with a README" 체크박스는 해제** (이미 로컬에 파일이 있으므로)
5. "Create repository" 클릭

## 2단계: 저장소 생성 후 알려주세요

저장소를 만든 후, 아래 명령어로 푸시하겠습니다:

```bash
git push -u origin main
```

또는 저장소를 만든 후 "완료"라고 알려주시면 자동으로 푸시하겠습니다.





